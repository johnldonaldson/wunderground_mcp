[project]
name = "wunderground"
version = "0.1.0"
description = "Weather Underground MCP server for current conditions and forecasts"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.2",
]
